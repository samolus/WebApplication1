
        @model IEnumerable<WebApplication1.Models.Movies>
    @{
            ViewBag.Title = "Index";
        }
    <body>
    @*<link href="projetfin.css" rel="stylesheet" type="text/css" media="all" />*@
    <h2 style="text-align:center; font-size:50px; color:white">Index of Movies</h2>
    <table style="border-spacing:40px 50px; font-weight:bold; color:black">
        @foreach (var item in Model)
        {

            <tr>
                <td>
                    @{
                        if (item.Image != null)
                        {
                            var base64 = Convert.ToBase64String(item.Image);
                            var imgSrc = String.Format("data:image/gif;base64,{0}", base64);
                        <img src="@imgSrc" />
                        }
                }                    
                </td>
                <td>
                    @item.Login.login
                </td>
                <td>
                  
                    @item.Name
                </td>
                <td>
                    @item.Categories
                </td>
                <td>
                    @item.Date
                </td>
                <td>
                    @item.Nationality
                </td>
                <td>
                    @item.Director
                </td>
                <td>
                    @item.Actors
                </td>
                <td style="font-size:12px; font-style:oblique">
                    @item.Resume
                </td>
                <td>
                    <form action="\Contact/DeleteMovies/@item.id" method="post">
                        <input class="btn" type="submit" value="Delete " name="Delete" />
                    </form>
                </td>
                <td>
                    <form action="\Contact/UpdateMovies/@item.id" method="post">
                        <input class="btn" type="submit" value="Update" name="UpdateMovies" />
                    </form>
                </td>
            </tr>

                        }
    </table>
        <footer style="text-align:center">
            <a href="\Home\Index"><button type="button" class="btn" style="font-size:20px; text-align:center">Home</button></a>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
            <a href="\Contact\CreateContact"><button type="button" class="btn" style="font-size:20px; text-align:center">Add a new Movie ?</button></a>
        </footer>
</body>
    @*<p></p>
        <p><a href="\Contact/createContact">Create Contact ?</a></p>*@

